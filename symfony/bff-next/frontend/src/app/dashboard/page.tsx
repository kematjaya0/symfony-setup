import { ListPageCard } from '@kematjaya/bootstrap-ui-kit';

export default function DashboardPage() {
    return (
        <ListPageCard
            title="Dashboard"
            subtitle="Welcome back. This is your starting point — wire up widgets here as features are added."
        >
            {null}
        </ListPageCard>
    );
}
